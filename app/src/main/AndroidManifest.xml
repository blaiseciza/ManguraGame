<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.blaiseciza.manguragame"
    android:versionCode="2"
    android:versionName="1.0">
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

     <!--<uses-sdk -->
     <!--android:minSdkVersion="7" />-->
    <!-- android:targetSdkVersion="21"/> -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/icoawale"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PageGame"
            android:screenOrientation="landscape">

            <!-- android:label="@string/title_activity_page_game" -->

        </activity>
        <activity
            android:name=".EndPage"
            android:screenOrientation="portrait">

            <!-- android:label="@string/title_activity_end_page" -->
        </activity>
        <activity
            android:name=".HelpActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".isigActivity"></activity>
    </application>

</manifest>